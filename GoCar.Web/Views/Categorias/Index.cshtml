@model List<GoCar.Web.Models.CategoriaViewModel>

@{
    ViewData["Title"] = "Categorias";
}

<div class="container py-5">

    <div class="d-flex justify-content-between align-items-center mb-4">

        <div>
            <h1>Categorias</h1>

            <p class="text-muted mb-0">
                Gerenciamento das categorias de veículos.
            </p>
        </div>

        <div class="d-flex gap-2">

            <a class="btn btn-dark"
               asp-controller="Categorias"
               asp-action="Criar">

                Nova Categoria
            </a>

            <a class="btn btn-outline-light"
               asp-controller="Dashboard"
               asp-action="Index">

                Voltar ao Dashboard
            </a>

        </div>

    </div>

    @if (TempData["Sucesso"] != null)
    {
        <div class="alert alert-success">
            @TempData["Sucesso"]
        </div>
    }

    @if (TempData["Erro"] != null)
    {
        <div class="alert alert-danger">
            @TempData["Erro"]
        </div>
    }

    @if (ViewBag.MensagemErro != null)
    {
        <div class="alert alert-danger">
            @ViewBag.MensagemErro
        </div>
    }

    <div class="card border-0 shadow-sm">

        <div class="card-body">

            <div class="table-responsive">

                <table class="table table-hover align-middle mb-0">

                    <thead>
                        <tr>
                            <th>ID</th>
                            <th>Nome</th>
                            <th>Descrição</th>
                            <th>Diária Base</th>
                            <th>KM Livre</th>
                            <th>Status</th>
                            <th>Ações</th>
                        </tr>
                    </thead>

                    <tbody>

                        @if (Model.Count == 0)
                        {
                            <tr>
                                <td colspan="7"
                                    class="text-center py-4">

                                    Nenhuma categoria encontrada.

                                </td>
                            </tr>
                        }
                        else
                        {
                            @foreach (var categoria in Model)
                            {
                                <tr>

                                    <td>
                                        @categoria.Id
                                    </td>

                                    <td>
                                        <strong>
                                            @categoria.Nome
                                        </strong>
                                    </td>

                                    <td>
                                        @categoria.Descricao
                                    </td>

                                    <td>
                                        @categoria.DiariaBase.ToString("C")
                                    </td>

                                    <td>
                                        @categoria.KmLivre km
                                    </td>

                                    <td>

                                        @if (categoria.IsAtivo)
                                        {
                                            <span class="badge bg-success">
                                                Ativa
                                            </span>
                                        }
                                        else
                                        {
                                            <span class="badge bg-secondary">
                                                Inativa
                                            </span>
                                        }

                                    </td>

                                    <td>

                                        <div class="d-flex flex-wrap gap-2">

                                            <a class="btn btn-sm btn-outline-dark"
                                               asp-controller="Categorias"
                                               asp-action="Detalhes"
                                               asp-route-id="@categoria.Id">

                                                Visualizar
                                            </a>

                                            <a class="btn btn-sm btn-dark"
                                               asp-controller="Categorias"
                                               asp-action="Editar"
                                               asp-route-id="@categoria.Id">

                                                Editar
                                            </a>

                                            @if (categoria.IsAtivo)
                                            {
                                                <form asp-controller="Categorias"
                                                      asp-action="Desativar"
                                                      asp-route-id="@categoria.Id"
                                                      method="post"
                                                      class="d-inline"
                                                      onsubmit="return confirm('Deseja realmente desativar esta categoria?');">

                                                    @Html.AntiForgeryToken()

                                                    <button type="submit"
                                                            class="btn btn-sm btn-danger">

                                                        Desativar
                                                    </button>

                                                </form>
                                            }
                                            else
                                            {
                                                <form asp-controller="Categorias"
                                                      asp-action="Ativar"
                                                      asp-route-id="@categoria.Id"
                                                      method="post"
                                                      class="d-inline"
                                                      onsubmit="return confirm('Deseja reativar esta categoria?');">

                                                    @Html.AntiForgeryToken()

                                                    <button type="submit"
                                                            class="btn btn-sm btn-success">

                                                        Ativar
                                                    </button>

                                                </form>
                                            }

                                        </div>

                                    </td>

                                </tr>
                            }
                        }

                    </tbody>

                </table>

            </div>

        </div>

    </div>

</div>